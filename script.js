const menuToggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav');if(menuToggle&&nav){menuToggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuToggle.setAttribute('aria-expanded','false');}));}const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();function handleContact(event){event.preventDefault();const note=document.getElementById('form-note');if(note)note.textContent='Thanks! The form is ready for your preferred order/contact service. For now, connect it to Formspree, Netlify Forms, or your bakery email before launch.';return false;}